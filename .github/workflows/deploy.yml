name: 강화중독 빌드 및 MonsterASP.NET 배포

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: windows-latest
    steps:
      - name: 소스 코드 받기
        uses: actions/checkout@v4

      - name: MSBuild 준비
        uses: microsoft/setup-msbuild@v2

      - name: Release 빌드
        run: msbuild EnhanceAddiction.WebForms.sln /t:Build /p:Configuration=Release

      - name: 배포 폴더 구성
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path publish | Out-Null
          Copy-Item Api, Auth, Content, Scripts, bin -Destination publish -Recurse
          Copy-Item Default.aspx, Global.asax, Web.config -Destination publish

      - name: MonsterASP.NET에 배포
        uses: rasmusbuchholdt/simply-web-deploy@2.2.0
        with:
          website-name: ${{ secrets.WEBSITE_NAME }}
          server-computer-name: ${{ secrets.SERVER_COMPUTER_NAME }}
          server-username: ${{ secrets.SERVER_USERNAME }}
          server-password: ${{ secrets.SERVER_PASSWORD }}
          source-path: '\publish\'
          target-delete: true
